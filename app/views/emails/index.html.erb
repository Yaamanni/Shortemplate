
<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Shortemplate</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">About</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#"> Upload</a>
        </li>
         <li class="nav-item">
          <a class="nav-link" href="#">Favourites </a>
        </li>
      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>

<H1> All EMAILS 🧠</H1>

<ul>

<div class="card">
  <div class="card-header">
    Shortemplate
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p>“Don't resist the growing pains, push through them. ”</p>
      <footer class="blockquote-footer">Germany Kent </cite></footer>
    </blockquote>
  </div>
</div>
<br>

</ul>
    <%= form_tag emails_path, method: :get do %>
  <%= text_field_tag :search, params[:search], placeholder: "Search emails" %>
  <%= submit_tag "Search", name: nil %>
  <% if params[:search].present? %>
    <%= link_to "Clear Search", emails_path %>
  <% end %>
<% end %>

<% if params[:search].present? %>
  <% emails = Email.where("subject LIKE :search OR content LIKE :search", search: "%#{params[:search]}%").order(created_at: :desc) %>
<% else %>
  <% emails = Email.all.order(created_at: :desc) %>
<% end %>

<ul>
  <% if emails.present? %>
    <% emails.each do |email| %>
      <li><h2><%= link_to email.subject, email_path(email) %></h2></li>
    <% end %>
  <% else %>
    <li>No emails found.</li>
  <% end %>
</ul>
